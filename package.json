{
  "name": "@hovoh/nestjs-environment-module",
  "version": "1.0.4",
  "description": "NestJs module to load a .env file and merge process env variables into a service",
  "author": "HOVOH",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run clean && tsc -p .",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@nestjs/common": "^7.6.13",
    "dotenv": "^8.2.0",
    "rxjs": "^6.6.7"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^18.0.0",
    "@types/node": "^14.14.37",
    "rimraf": "^3.0.2",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.5"
  }
}
